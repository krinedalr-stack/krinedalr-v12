// components/Weather.jsx

export default function Weather() {
  return (
    <div className="weather-widget">
      <div className="weather-title">Today’s Readiness Snapshot</div>
      <p>
        We check Met Éireann updates before going on any roof. If warnings move
        from green to yellow, orange or red, we adjust how and when we work so
        the job stays safe – but your property is still protected.
      </p>
    </div>
  );
}
